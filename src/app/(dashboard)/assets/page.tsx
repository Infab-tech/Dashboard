import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function AssetsPage() {
  return (
    <PagePlaceholder
      title="Assets"
      description="Equipment and fixed assets, their status, and who they're currently assigned to."
    />
  );
}
