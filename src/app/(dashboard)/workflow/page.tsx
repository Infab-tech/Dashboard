import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function WorkflowPage() {
  return (
    <PagePlaceholder
      title="Workflow"
      description="Task stages and dependencies per project, tracked from To Do through Done."
    />
  );
}
