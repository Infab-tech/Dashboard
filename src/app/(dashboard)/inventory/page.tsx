import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function InventoryPage() {
  return (
    <PagePlaceholder
      title="Inventory"
      description="Stock items across projects and locations, each with a unique ID and quantity tracking."
    />
  );
}
