import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function AdminPage() {
  return (
    <PagePlaceholder
      title="Admin"
      description="Global head controls — manage users, review system-wide activity, and configure ID formats per module."
    />
  );
}
