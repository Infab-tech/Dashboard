import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function DashboardHomePage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Global overview across all projects — status, alerts, and key metrics for the Admin."
    />
  );
}
