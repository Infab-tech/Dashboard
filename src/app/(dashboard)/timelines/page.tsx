import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function TimelinesPage() {
  return (
    <PagePlaceholder
      title="Timelines"
      description="Milestones and schedules across each project's lifecycle."
    />
  );
}
