import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function PeoplePage() {
  return (
    <PagePlaceholder
      title="People"
      description="Who reports to whom, per project — the people-on-people dependency tree."
    />
  );
}
